# ContactLogo — audit concurrence (2026-09-01)

Source: owner desktop report plus live probes, five expert subagents, and the tree at `grok/full-stack-audit`.

## Verdict

Concur with the report's headline: the matching engine is no longer the risk.  Trust, licensing, and "the notification opens a real queue" are the risk.  The site is a working review tool, not store-ready.

## Corrections to the desktop report

| Claim | Verdict |
| --- | --- |
| Datadog `trackUserInteractions: true`, 100% sample | **Correct.**  Now `false`, session sample 20%, Session Replay 0.  CL-23 is mitigated. |
| Tighten `connect-src` to Google + telemetry only | **Correct.**  `embedSrc()` still fetches logo CDNs for export.  `csp.test.ts` will fail if those origins leave `connect-src`.  Paste-URL already uses `img-src https:` only. |
| 23 of 79 Simple Icons slugs 404 | **Expand.**  Live `cdn.simpleicons.org` on 2026-09-01: **22 dead, 57 live**.  `salesforce` recovered.  `jpmorgan` is 404; **`chase` is 200** — remap, do not drop Chase. |
| Web Settings hosts Brandfetch / Logo.dev keys | **Correct.**  Those keys are Vite env on web.  Native Settings is the key UI. |
| Footer clipped in source | **Likely viewport, not CSS clip.**  `.app` has 80px bottom padding.  Full-page mobile screenshot shows the footer. |
| Crop modal a11y missing | **Partly stale.**  Dialog, Escape, and focus trap exist.  Canvas still lacks an accessible name and keyboard pan. |
| Clearbit unreachable | **Concur** from this network (HTTP 000).  Not proof Clearbit is dead. |

## Concurrence (still true)

- P0-1 licensing unresolved.  Simple Icons is deleting the marks this product depends on.
- P0-2 no App Store / TestFlight / Play listing.  iTunes lookup `resultCount: 0`.
- P0-3 privacy policy did not match binaries (Android omitted, telemetry omitted, logo hosts incomplete).  **Rewritten in this lane.**
- P0-4 iOS overnight notification vs in-memory queue (#32).  Still open.
- P1-1 stale Simple Icons map.  **Dropped/remapped in this lane.**
- P1-2 no first-party logo store.  Still the right design: domain-keyed PNG cache, never address books.
- P1-3 Android has no undo.  Apply can delete the prior photo first.
- P1-4 Android UI subset (wrap, Ready chip side effect, generic icon, no override).
- P1-5 native retryable rows look like Not found (#33).
- P1-6 host story.  **Declared: Vercel is production.**
- P2 SEO/PWA/theme-color/privacy palette/"this phone" copy.  **Fixed in this lane.**
- P2-3 virtualizer first-row height (#35).  Still open.
- #36 brand-tail vs email domain.  Product call; capped at medium.  Leave as specified until owner chooses.

## New findings from the expert team

- Web Sentry DSN would be blocked by CSP without ingest hosts.  **Allowlisted in this lane.**
- Double `serviceWorker.register`.  **Duplicate in `main.ts` removed.**
- Fleet light-default: OS dark was booting the product.  **`data-theme="light"` now.**
- `--skip` contrast was the old exhausted-ink failure.  **Light skip darkened.**
- iOS Sentry DSN is hardcoded with `environment = "production"`.  Public DSN is expected; still no Mac Sentry.
- Android `applyPhoto` deletes then inserts; failures are silent; WRITE_CONTACTS is not gated.
- Ticker pack (`davidepalazzo/ticker-logos`) has no LICENSE file.  Treat as review-only until legal reads it.
- Android theme follows system dark.  Same light-default debt as web had.
- `support@contactlogo.com` still unconfirmed.

## Owner decisions still blocking store

1. Which logo sources may be auto-applied (inlined SVGs + live Simple Icons + ticker pack after license read is the recommended default).
2. Confirm the support mailbox.
3. Brandfetch / Logo.dev keys via Infisical → Vercel, or accept medium-only long tail.
4. Device apply/undo on a throwaway address book (FedEx purple, Delta visible, undo across two batches).
5. ASC + Play records only after 1–4.
